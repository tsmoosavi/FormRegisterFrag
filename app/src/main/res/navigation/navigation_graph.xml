<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/firstFragment">

    <fragment
        android:id="@+id/firstFragment"
        android:name="com.example.formregisterfrag.FirstFragment"
        android:label="fragment_first"
        tools:layout="@layout/fragment_first" >
        <action
            android:id="@+id/action_firstFragment_to_secondeFragment"
            app:destination="@id/secondeFragment" />
    </fragment>
    <fragment
        android:id="@+id/secondeFragment"
        android:name="com.example.formregisterfrag.SecondeFragment"
        android:label="fragment_seconde"
        tools:layout="@layout/fragment_seconde" >
        <action
            android:id="@+id/action_secondeFragment_to_firstFragment"
            app:destination="@id/firstFragment" />
        <argument
            android:name="name"
            app:argType="string" />
        <argument
            android:name="userName"
            app:argType="string" />
        <argument
            android:name="email"
            app:argType="string" />
        <argument
            android:name="passWord"
            app:argType="string" />
        <argument
            android:name="gender"
            app:argType="string" />
    </fragment>
</navigation>